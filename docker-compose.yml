version: "3.9"

services:
  trivia_app:
    build: .
    container_name: trivia_app
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # keep SQLite DB persistent
    restart: unless-stopped
